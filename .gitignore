# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.installed.cfg

# --- Virtual environments ---
legal-anon-env/
.venv/
venv/
env/
ENV/
demo/venv-gradio/

# --- HuggingFace Spaces sync artefacts (regenerated by spaces/sync.sh) ---
spaces/anonymisation/
spaces/examples/

# --- Local HF Spaces clone — nested git repo, not part of the main project ---
# Use `./spaces/sync.sh` followed by `cp -r spaces/* Anonymiser/` to refresh it.
Anonymiser/

# --- Jupyter ---
.ipynb_checkpoints/
*.ipynb_checkpoints

# --- HuggingFace + spaCy caches ---
.cache/
~/.cache/huggingface/
.huggingface/

# --- Model artefacts (too large for git) ---
checkpoints/
models/
*.pt
*.bin
*.safetensors

# --- Data caches (TAB is fetched from HF on demand) ---
data/raw/
data/cache/

# --- Phase 6.2 prediction cache (4.5MB, reproducible via scripts/cache_predictions.py) ---
phase6_advanced_training/results/predictions_cache.json

# --- macOS ---
.DS_Store

# --- IDE ---
.vscode/
.idea/
*.swp

# --- Local secrets / env ---
.env
.env.local

# --- OS ---
Thumbs.db

# --- Interview prep (local only) ---
INTERVIEW_PLAN.md
INTERVIEW_STORIES.md
logs/
