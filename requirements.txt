# Core NLP / NER
spacy==3.7.4

# Dataset loading
datasets==2.18.0
huggingface-hub>=0.20.0

# Data wrangling + viz
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.13.0

# Notebook
jupyter>=1.0.0
ipykernel>=6.25.0

# --- Phase 2 — baseline comparison + fine-tuning ---
transformers>=4.40.0,<5.0.0
torch>=2.2.0
accelerate>=0.27.0
evaluate>=0.4.0
seqeval>=1.2.2
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
scikit-learn>=1.3.0

# --- Dev / tests ---
pytest>=7.0.0

# --- Phase 3 (will be enabled when Phase 3 work begins) ---
# fastapi>=0.110.0
# uvicorn>=0.29.0
# pydantic>=2.6.0

# After install, also run:
#   python -m spacy download en_core_web_trf       # spaCy baseline (Phase 1)
#   python -m spacy download en_core_web_lg        # required by Presidio's default NLP engine
