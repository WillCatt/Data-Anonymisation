# HuggingFace Spaces — pinned to versions known to coexist cleanly.
# The gradio dep tree historically conflicts with spaCy 3.7 if left
# unpinned (typer + huggingface-hub versions). These are the safe ones.

gradio>=4.20.0,<5.0.0
huggingface-hub>=0.19.0,<0.27.0
typer>=0.3.0,<0.10.0
spacy==3.7.4

# Pin the spaCy model as a direct wheel rather than relying on
# `python -m spacy download` — its compatibility-table lookup can resolve to a
# broken URL and 404 the build. This is the deterministic, HF-recommended way.
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl

# Dataset loading for the mosaic scorer
datasets==2.18.0
pandas>=2.0.0
